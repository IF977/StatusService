<%= render 'header' %>
<ol class="breadcrumb">
  <li><a href=<%="/users/" << current_user.id.to_s%>>Home</a></li>
</ol>
<div id="page-wrapper">
	<div class="container-fluid">
		<div class="row">
			<div class="col-lg-12">
                <h1 class="page-header">Perfil de <%= @user.nome %> </h1>
                Email:<%= @user.email %>
                Login:<%= @user.login %> 
                <% if current_user == @user %>
                <% end %>
            </div>
        </div>
    </div>
</div>
